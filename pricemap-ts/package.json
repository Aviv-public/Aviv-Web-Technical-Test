{
  "private": true,
  "version": "0.0.1",
  "main": "dist/index.js",
  "dependencies": {
    "express": "^4.17.3",
    "postgres": "^3.2.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^16.11.59",
    "nodemon": "^2.0.20",
    "typescript": "^4.8.3",
    "concurrently": "^7.4.0"
  },
  "scripts": {
    "watch": "PGHOST=db PGDATABASE=pricemap PGUSER=pricemap PGPASSWORD=pricemap concurrently -k -n \"Typescript,Node\" -p \"[{name}]\" -c \"blue,green\" \"tsc -b . --watch\" \"nodemon --delay 1 --inspect dist/index.js\"",
    "build": "tsc -b .",
    "start": "node dist/index.js"
  }
}
